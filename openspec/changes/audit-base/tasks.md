# Tasks: Audit Base — API, Frontend y Retrofit Quality Views

## Phase 1: Backend Serializer + View + URLs

- [x] **T1.1** — Create `backend/apps/audit/serializers.py` — define `AuditEventSerializer(ModelSerializer)` with fields `["id", "action", "level", "actor_email", "metadata", "created_at"]` all read-only; `actor_email` via `SerializerMethodField` returning `obj.actor.email if obj.actor else None`. Done when `AuditEventSerializer(audit_event_instance).data` returns all six fields with correct values.
- [x] **T1.2** — Create `AuditPagination` class in `backend/apps/audit/views.py` — extend `PageNumberPagination` with `page_size = 50`. Done when instantiating `AuditPagination()` produces an object with `page_size == 50`.
- [x] **T1.3** — Create `AuditListView(APIView)` in `backend/apps/audit/views.py` — `authentication_classes = [TokenAuthentication]`, `permission_classes = [IsAuthenticated]`; `get()` calls `get_request_membership(request)` and returns 403 if `membership` is `None` or `membership.role.code not in {"global_admin", "tenant_admin"}`. Done when calling the view with a `monitor` token returns HTTP 403.
- [x] **T1.4** — Add queryset + inline filters to `AuditListView.get()` in `backend/apps/audit/views.py` — base queryset `AuditEvent.objects.filter(tenant=membership.tenant).order_by("-created_at")`; apply `action`, `level`, `date_from` (`created_at__date__gte`), `date_to` (`created_at__date__lte`) from `request.GET` conditionally. Done when `?action=lots.create` returns only matching events.
- [x] **T1.5** — Add pagination + response to `AuditListView.get()` in `backend/apps/audit/views.py` — instantiate `AuditPagination`, call `paginate_queryset(qs, request)`, serialize with `AuditEventSerializer(page, many=True)`, return `paginator.get_paginated_response(serializer.data)`. Done when response contains keys `count`, `next`, `previous`, `results`.
- [x] **T1.6** — Create `backend/apps/audit/urls.py` — define `urlpatterns = [path("", AuditListView.as_view(), name="audit-list")]`. Done when `python manage.py show_urls | grep audit` lists the route.
- [x] **T1.7** — Modify `backend/config/urls.py` — add `path("api/v1/audit/", include("apps.audit.urls"))` to `urlpatterns`. Done when `GET /api/v1/audit/` returns HTTP 401 (not 404) without auth token.

## Phase 2: Quality Retrofit

- [x] **T2.1** — Modify `backend/apps/quality/views.py` — add `from apps.audit.services import log_audit_event` import; in `LotListCreateView.post()`, after `serializer.save()`, call `log_audit_event(action="lots.create", tenant=membership.tenant, actor=request.user, metadata={"lot_id": lot.id, "code": lot.code})`. Done when a successful `POST /api/v1/lots/` creates an `AuditEvent` with `action="lots.create"`.
- [x] **T2.2** — Modify `backend/apps/quality/views.py` — in `LotDetailView.patch()`, after `serializer.save()`, call `log_audit_event(action="lots.update", tenant=membership.tenant, actor=request.user, metadata={"lot_id": lot.id})`. Done when a successful `PATCH /api/v1/lots/{id}/` creates an `AuditEvent` with `action="lots.update"`.
- [x] **T2.3** — Modify `backend/apps/quality/views.py` — in `LotStatusView.patch()`, after saving the new status, call `log_audit_event(action="lots.change_status", tenant=membership.tenant, actor=request.user, metadata={"lot_id": lot.id, "new_status": lot.status})`. Done when a successful `PATCH /api/v1/lots/{id}/status/` creates an `AuditEvent` with `action="lots.change_status"`.
- [x] **T2.4** — Verify `python manage.py check` in `backend/` — exits 0 with no errors after all three retrofit changes. Done when the command output shows `System check identified no issues (0 silenced)`.

## Phase 3: Frontend Integration

- [x] **T3.1** — Create `frontend/lib/audit/types.ts` — export `AuditLevel` type (`"info" | "warning" | "error"`), `AuditEvent` interface (fields: `id`, `action`, `level`, `actor_email`, `metadata`, `created_at`), `AuditFilters` interface (optional: `action`, `level`, `date_from`, `date_to`, `page`), `PaginatedAuditResponse` interface (`count`, `next`, `previous`, `results`). Done when TypeScript compilation passes with no errors referencing this file.
- [x] **T3.2** — Create `frontend/lib/audit/api.ts` — export `listAuditEvents(params?: AuditFilters): Promise<PaginatedAuditResponse>` using the project's `apiRequest` helper; build `URLSearchParams` from `params` and append to `/api/v1/audit/` only for defined values. Done when all exports are present and TypeScript compiles cleanly.
- [x] **T3.3** — Create `frontend/hooks/use-audit.ts` — implement `useAudit(filters?: AuditFilters)` hook following the `useLots()` pattern (`useState + useCallback + useEffect + useMemo`); expose `UseAuditReturn` with `events`, `count`, `isLoading`, `error`, `page`, `setPage`, `filters`, `setFilters`, `refresh`; re-fetch when `filters` or `page` change via `useEffect`. Done when hook satisfies `UseAuditReturn` and TypeScript compiles cleanly.
- [x] **T3.4** — Create `frontend/app/dashboard/audit/page.tsx` — client component; on mount check role from `AuthContext` and redirect to `/dashboard` if not `global_admin` or `tenant_admin` (guard runs before `useAudit()` is called); render filter controls (action text input, level select, date_from/date_to date inputs, clear-all button) that update `useAudit` params; render Shadcn `<DataTable>` with columns: `created_at` (formatted), `actor_email`, `action`, `level`, `metadata` (truncated); show loading skeleton when `isLoading`, empty-state message when `events` is empty and no error, error message when `error` is set; no edit/delete controls. Done when `/dashboard/audit` renders for `tenant_admin`, redirects for `monitor`, and table is read-only.

## Phase 4: Backend Tests

- [x] **T4.1** — Create `backend/tests/test_audit.py` — add `@pytest.mark.django_db` fixtures: `audit_event_factory` helper that creates `AuditEvent(tenant, actor, action, level, metadata)` for a given tenant; reuse `admin_client`, `monitor_client`, `tenant`, `tenant_b`, `seeded_roles` from `conftest.py`. Done when fixtures are importable and produce correct model instances.
- [x] **T4.2** — In `backend/tests/test_audit.py` — write `test_list_audit_tenant_admin_200`: `GET /api/v1/audit/` with `admin_client` returns HTTP 200 and response body contains keys `count`, `next`, `previous`, `results`. Done when assert passes.
- [x] **T4.3** — In `backend/tests/test_audit.py` — write `test_list_audit_global_admin_200`: create a membership with role `global_admin`; `GET /api/v1/audit/` returns HTTP 200. Done when assert passes.
- [x] **T4.4** — In `backend/tests/test_audit.py` — write `test_list_audit_unauthenticated_401`: `GET /api/v1/audit/` with no `Authorization` header returns HTTP 401. Done when assert passes.
- [x] **T4.5** — In `backend/tests/test_audit.py` — write `test_list_audit_monitor_403`: `GET /api/v1/audit/` with `monitor_client` returns HTTP 403. Done when assert passes.
- [x] **T4.6** — In `backend/tests/test_audit.py` — write `test_list_audit_tenant_isolation`: create 3 events for `tenant` and 2 events for `tenant_b`; `GET /api/v1/audit/` with `admin_client` (scoped to `tenant`) returns `count == 3` and no event in `results` has `tenant_id` matching `tenant_b`. Done when assert passes.
- [x] **T4.7** — In `backend/tests/test_audit.py` — write `test_list_audit_filter_by_action`: create 4 events with `action="lots.create"` and 3 with `action="users.create"` for the same tenant; `GET /api/v1/audit/?action=lots.create` returns `count == 4` and all items in `results` have `action="lots.create"`. Done when assert passes.
- [x] **T4.8** — In `backend/tests/test_audit.py` — write `test_audit_logged_on_lot_create`: `POST /api/v1/lots/` with a write-capable token and valid body returns HTTP 201; then assert `AuditEvent.objects.filter(action="lots.create", tenant=tenant).count() == 1`. Done when assert passes.
- [x] **T4.9** — In `backend/tests/test_audit.py` — write `test_audit_logged_on_lot_status_change`: create a `pending` lot; `PATCH /api/v1/lots/{id}/status/` with `{status: "in_process"}` returns HTTP 200; assert `AuditEvent.objects.filter(action="lots.change_status").count() == 1`. Done when assert passes.
- [x] **T4.10** — Run `pytest backend/tests/test_audit.py -v` — all tests must pass (T4.2–T4.9). Done when output shows `0 failed`.

## Phase 5: Docs

- [x] **T5.1** — Update `openspec/changes/audit-base/design.md` — add ✅ Status column to the File Changes table marking each implemented file; resolve Open Questions: document that `AuditListView` always filters by `membership.tenant` (no cross-tenant superadmin), and that `actor_email` alone is sufficient for this iteration. Done when all rows in the table have a status and both Open Questions are answered.
- [x] **T5.2** — Update `docs/analisis/estado-proyecto.md` — add entry for the Audit Base change: note that `GET /api/v1/audit/` endpoint, quality retrofit (lots.create / lots.update / lots.change_status), and frontend `/dashboard/audit` page are operational; update overall completion percentage for the audit module. Done when the file reflects the new state.
- [ ] **T5.3** — Mark all tasks complete in `openspec/changes/audit-base/tasks.md` — change every `- [ ]` to `- [x]` after implementation is verified. Done when `grep -c "\- \[ \]" tasks.md` returns 0.
