# Tasks: Lots Vertical Slice — ColdevConAC

## Phase 1: Backend Model + Migration

- [x] **T1.1** — Add `Lot` model to `backend/apps/quality/models.py` — fields: `id`, `tenant` (FK → `TenantRegistry`, `CASCADE`), `code` (`CharField(max_length=30, blank=True)`), `species` (`CharField(max_length=100)`), `origin` (`CharField(max_length=150)`), `entry_date` (`DateField`), `status` (`CharField`, `TextChoices`: `pending/in_process/approved/rejected`, default `pending`), `quantity_kg` (`DecimalField(max_digits=10, decimal_places=2)`), `created_by` (FK → `settings.AUTH_USER_MODEL`, `SET_NULL`, `null=True`), `created_at` (`auto_now_add`), `updated_at` (`auto_now`); `Meta.unique_together = ("code", "tenant")`; `Meta.ordering = ["-entry_date"]`. Remove `QualityPlaceholder`. Done when `python manage.py check` exits 0.
- [x] **T1.2** — Override `Lot.save()` in `backend/apps/quality/models.py` — auto-generate `code` in format `LOT-{year}-{seq:03d}` (per tenant per year) when `code` is blank, using `Lot.objects.filter(tenant=self.tenant, entry_date__year=year).count() + 1` as sequence. Done when creating a lot with no code produces a non-blank code matching the pattern.
- [x] **T1.3** — Generate migration `python manage.py makemigrations quality --name add_lot` in `backend/apps/quality/migrations/0002_add_lot.py` — migration must `DeleteModel("QualityPlaceholder")` then `CreateModel("Lot")` with all fields and `unique_together`. Done when `python manage.py migrate quality` exits 0.
- [x] **T1.4** — Register `Lot` in `backend/apps/quality/admin.py` — `@admin.register(Lot)` with `list_display = ["code", "species", "tenant", "status", "entry_date", "quantity_kg"]` and `list_filter = ["status", "tenant"]`. Done when the model is visible in the Django admin.

## Phase 2: Backend API Endpoints

- [x] **T2.1** — Create `backend/apps/quality/permissions.py` — define `LOT_WRITE_ROLES = {"manager", "tenant_admin", "global_admin", "quality_manager"}` and `lot_can_write(membership) -> bool` returning `True` if `membership.role.code in LOT_WRITE_ROLES`. Done when `lot_can_write` returns `True` for `manager` and `False` for `monitor`.
- [x] **T2.2** — Create `backend/apps/quality/serializers.py` with `LotSerializer` (read) — `ModelSerializer` for `Lot`; fields: `id`, `code`, `species`, `origin`, `entry_date`, `status`, `quantity_kg`, `created_by_username` (`SerializerMethodField`, read-only), `created_at`, `updated_at`. Done when `LotSerializer(lot_instance).data` returns all fields.
- [x] **T2.3** — Add `LotCreateSerializer` to `backend/apps/quality/serializers.py` — `Serializer` with fields: `code` (optional, blank allowed), `species`, `origin`, `entry_date`, `quantity_kg`; `validate_code()` checks uniqueness against `context["tenant"]`; `create()` saves `Lot` with `tenant` and `created_by` from context. Done when posting a duplicate code returns `ValidationError`.
- [x] **T2.4** — Add `LotStatusSerializer` to `backend/apps/quality/serializers.py` — `Serializer` with `status` (`ChoiceField`); `validate_status()` enforces `ALLOWED_TRANSITIONS` (`pending→{in_process, rejected}`, `in_process→{approved, rejected}`, `approved→∅`, `rejected→∅`) against `context["lot"].status`, raising `ValidationError` on invalid transitions. Done when `pending → approved` raises `ValidationError`.
- [x] **T2.5** — Create `backend/apps/quality/views.py` with `LotListCreateView(APIView)` — `get()`: call `get_request_membership(request)`, filter `Lot.objects.filter(tenant=membership.tenant).select_related("created_by").order_by("-entry_date")`, return `LotSerializer(qs, many=True).data` with HTTP 200. `post()`: additionally check `lot_can_write(membership)` (403 otherwise), validate `LotCreateSerializer(data, context={tenant, user})`, call `log_audit_event("lots.create", ...)`, return HTTP 201. Done when `GET` and `POST` both respond correctly in manual test.
- [x] **T2.6** — Add `LotDetailView(APIView)` to `backend/apps/quality/views.py` — `get()` returns single lot (200) scoped to `membership.tenant`; `patch()` checks `lot_can_write` (403), validates editable fields (`species`, `origin`, `entry_date`, `quantity_kg`) via `LotCreateSerializer(lot, data, partial=True, context=...)`, logs `lots.update`, returns 200; both return 404 if lot not in tenant. Done when cross-tenant PATCH returns 404.
- [x] **T2.7** — Add `LotStatusView(APIView)` to `backend/apps/quality/views.py` — `patch()`: resolve membership, check `lot_can_write` (403), get lot scoped to tenant (404 if not found), validate `LotStatusSerializer(data, context={"lot": lot})`, save new status, log `lots.change_status`, return 200 with `LotSerializer`. Done when `pending → approved` returns HTTP 400.
- [x] **T2.8** — Create `backend/apps/quality/urls.py` — define `urlpatterns` with three explicit patterns: `path("", LotListCreateView.as_view())`, `path("<int:lot_id>/", LotDetailView.as_view())`, `path("<int:lot_id>/status/", LotStatusView.as_view())`. Done when `python manage.py show_urls | grep lots` lists all three routes.
- [x] **T2.9** — Modify `backend/config/urls.py` — add `path("api/v1/lots/", include("apps.quality.urls"))` to `urlpatterns`. Done when `GET /api/v1/lots/` returns 401 (not 404) without auth.

## Phase 3: Frontend Integration

- [x] **T3.1** — Create `frontend/lib/lots/types.ts` — export `LotStatus` (`"pending" | "in_process" | "approved" | "rejected"`), `Lot` interface (all 10 fields as per design contract), `CreateLotInput` interface (`code?`, `species`, `origin`, `entry_date`, `quantity_kg`), `UpdateLotInput` interface (all fields optional). Done when TypeScript compilation passes with no errors referencing this file.
- [x] **T3.2** — Create `frontend/lib/lots/api.ts` — export four functions using the project's `apiRequest` helper: `listLots(token)`, `createLot(token, input: CreateLotInput)`, `updateLot(token, id, input: UpdateLotInput)`, `changeLotStatus(token, id, status: LotStatus)`. Each maps to its respective endpoint. Done when all functions are exported and TypeScript compiles cleanly.
- [x] **T3.3** — Create `frontend/hooks/use-lots.ts` — implement `useLots()` hook mirroring `use-users.ts` pattern (`useState + useCallback + useEffect + useMemo`); expose `UseLotsReturn` interface with `lots`, `isLoading`, `error`, `refresh`, `createLot`, `updateLot`, `changeLotStatus`; read auth token from `AuthContext`; call `refresh()` on mount via `useEffect`. Done when hook satisfies `UseLotsReturn` and TypeScript compiles clean.
- [x] **T3.4** — Create `frontend/components/lots/lot-status-badge.tsx` — render color-coded badge per status: `pending`→yellow (`bg-yellow-100 text-yellow-700` / "Pendiente"), `in_process`→blue ("En Proceso"), `approved`→green ("Aprobado"), `rejected`→red ("Rechazado"); fallback to neutral gray for unknown values without throwing. Done when all 4 statuses render correct classes and labels.
- [x] **T3.5** — Create `frontend/components/lots/lot-form.tsx` — Shadcn `Dialog` form with fields: `code` (optional), `species` (required), `origin` (required), `entry_date` (required, date input), `quantity_kg` (required, numeric); client-side required-field validation before submit; calls `useLots().createLot(data)` on submit; closes modal on success; displays field-level API error messages on 400 response. Done when submitting empty `species` shows inline error without calling `createLot`.
- [x] **T3.6** — Create `frontend/app/dashboard/lots/page.tsx` — server/client page rendering `useLots()` data in a responsive Shadcn `Table` with columns: Código, Especie, Origen, Fecha Entrada, Cantidad (Kg), Estado; loading indicator while `isLoading`; error message when `error` is set; empty-state message "No hay lotes registrados" when `lots` is empty; "Nuevo Lote" button that opens `LotForm` dialog, visible only for write-capable roles from `AuthContext`. Done when the page renders correctly in `next dev` and the button is hidden for `monitor` role.
- [x] **T3.7** — Refactor `frontend/components/quality-manager/lot-management.tsx` — replace hardcoded mock `Lot[]` and legacy statuses (`active`, `processing`, `completed`, `on-hold`) with `useLots()` hook data; replace any internal `getLotBadge` using old statuses with `LotStatusBadge` component; align local interface with the real `Lot` type from `lib/lots/types.ts`. Done when `grep -n "active\|processing\|completed\|on-hold"` returns no matches in the file.

## Phase 4: Backend Tests

- [x] **T4.1** — Create `backend/tests/test_lots.py` — add fixtures `write_role` (code `"manager"`), `readonly_role` (code `"monitor"`), `write_membership`, `readonly_membership`, and `lot_factory` helper that creates a `Lot` for a given tenant. Done when fixtures are importable and produce correct model instances.
- [x] **T4.2** — In `backend/tests/test_lots.py` — write `test_list_lots_authenticated`: `GET /api/v1/lots/` with `write_membership` token returns 200 and a list with exactly the lots belonging to that tenant. Done when assert passes.
- [x] **T4.3** — In `backend/tests/test_lots.py` — write `test_list_lots_unauthenticated`: `GET /api/v1/lots/` with no token returns 401. Done when assert passes.
- [x] **T4.4** — In `backend/tests/test_lots.py` — write `test_list_lots_tenant_isolation`: create 2 lots for tenant A and 3 for tenant B; `GET /api/v1/lots/` with tenant A token returns exactly 2 lots and none with `tenant_id` matching tenant B. Done when assert passes.
- [x] **T4.5** — In `backend/tests/test_lots.py` — write `test_create_lot_valid_returns_201`: `POST /api/v1/lots/` with `write_membership` and valid body `{species, origin, entry_date, quantity_kg}` returns 201; response body has `status: "pending"` and a non-blank `code`; an audit event `lots.create` is recorded. Done when all three assertions pass.
- [x] **T4.6** — In `backend/tests/test_lots.py` — write `test_create_lot_missing_species_returns_400`: `POST /api/v1/lots/` without `species` field returns 400 and identifies `species` in response body; no `Lot` is created. Done when assert passes.
- [x] **T4.7** — In `backend/tests/test_lots.py` — write `test_create_lot_monitor_returns_403`: `POST /api/v1/lots/` with `readonly_membership` token returns 403. Done when assert passes.
- [x] **T4.8** — In `backend/tests/test_lots.py` — write `test_get_lot_detail_own_tenant`: `GET /api/v1/lots/{lot.id}/` returns 200 with all required fields including `code`, `status`, `created_by_username`. Done when assert passes.
- [x] **T4.9** — In `backend/tests/test_lots.py` — write `test_get_lot_detail_cross_tenant_returns_404`: create lot for tenant B; `GET /api/v1/lots/{lot_b.id}/` with tenant A token returns 404. Done when assert passes.
- [x] **T4.10** — In `backend/tests/test_lots.py` — write `test_update_lot_valid_returns_200`: `PATCH /api/v1/lots/{lot.id}/` with `{quantity_kg: 999}` returns 200 and `quantity_kg` is updated; `lots.update` audit event is recorded. Done when assert passes.
- [x] **T4.11** — In `backend/tests/test_lots.py` — write `test_update_lot_supervisor_returns_403`: `PATCH /api/v1/lots/{lot.id}/` with a `production_supervisor` membership returns 403. Done when assert passes.
- [x] **T4.12** — In `backend/tests/test_lots.py` — write `test_change_status_valid_transition`: `PATCH /api/v1/lots/{lot.id}/status/` with `{status: "in_process"}` on a `pending` lot returns 200 and the lot's `status` is `"in_process"`; `lots.change_status` audit event is recorded. Done when assert passes.
- [x] **T4.13** — In `backend/tests/test_lots.py` — write `test_change_status_invalid_transition_returns_400`: `PATCH /api/v1/lots/{lot.id}/status/` with `{status: "approved"}` on a `pending` lot returns 400 and the lot status remains `"pending"`. Done when assert passes.
- [x] **T4.14** — In `backend/tests/test_lots.py` — write `test_change_status_terminal_state_returns_400`: `PATCH /api/v1/lots/{lot.id}/status/` on an `approved` lot returns 400 for any `status` value. Done when assert passes.
- [x] **T4.15** — Run `pytest backend/tests/test_lots.py -v` — all 14 tests must pass (T4.2–T4.14 + fixtures). Done when output shows `14 passed, 0 failed`.

## Phase 5: Docs + Smoke

- [x] **T5.1** — Update `openspec/changes/lots-vertical-slice/design.md` — add ✅ Status column to the File Changes table marking each implemented file; resolve Open Questions with implementation decisions (e.g., confirm shared-DB architecture, document `LOT-{year}-{seq}` counter strategy). Done when all rows in the table have a status and no Open Question is unresolved.
- [x] **T5.2** — Update `docs/analisis/estado-proyecto.md` — add entry for the Lots vertical slice: note that `Lot` model, API (`/api/v1/lots/`), and frontend page (`/dashboard/lots/`) are operational; update overall completion percentage for the quality module. Done when the file reflects the new state.
- [ ] **T5.3** — Backend smoke test: `python manage.py runserver`; use `curl` or Postman to verify `GET /api/v1/lots/` → 401 (no token), `POST /api/v1/lots/` with valid token → 201, `GET /api/v1/lots/{id}/` → 200, `PATCH /api/v1/lots/{id}/status/` with invalid transition → 400. Done when all four responses match expected HTTP codes.
- [ ] **T5.4** — Frontend smoke test: `next dev`; log in as `manager` → navigate to `/dashboard/lots/` → verify table loads, "Nuevo Lote" button visible, form submission creates a new row without page reload; log in as `monitor` → verify "Nuevo Lote" button is absent. Done when both role scenarios work as described with zero console errors.
